# User-specific files and settings generated by IDEs.
# These should not be in source control as they are specific to each developer's machine.
*.suo
*.user
*.userosscache
*.sln.docstates

# Build output folders.
# These files are generated by the compiler and should not be stored in the repo.
# They are rebuilt every time someone clones and builds the project.
[Bb]in/
[Oo]bj/

# Visual Studio Code specific files
.vscode/

# Rider specific files
.idea/

# Mac specific files
.DS_Store

# Log and temporary files
*.log
*.tmp
*.temp

# NuGet packages folder.
# The .csproj file lists the packages needed; 'dotnet restore' downloads them.
# Storing these in the repo would add massive bloat.
[Pp]ackages/
*.nupkg

# Secrets file (ASP.NET Core)
# This file is designed to hold sensitive data like API keys and connection strings locally.
# It should NEVER be committed to source control.
secrets.json
user-secrets.json

# Test results and code coverage files
TestResults/
*.coverage
*.coveragereport